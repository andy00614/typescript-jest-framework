import { searchValueIntwoDimensionsArr, find } from "../search";

// beforeEach(() => {
//   targetArr = renderNewTwoDimensionArr(unitLength, length);
// });

// test("生成二维数组的方法", () => {
//   expect(targetArr.length).toBe(length / unitLength);
// });

// test("二维数组中的查找:小肚", () => {
//   expect(find(targetArr, 60)).toBe(true);
// });

// test("二维数组中的查找:andy", () => {
//   expect(searchValueIntwoDimensionsArr(targetArr, 60)).toBe(true);
// });
